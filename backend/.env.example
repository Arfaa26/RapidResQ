PORT=5000
# Optional Google Gemini API Key for deep multimodal visual AI triage
# GEMINI_API_KEY=your_gemini_api_key_here
# Required on Vercel: connect Neon in project Storage to populate this variable.
# Optional locally; without it, reports use the in-memory demo store.
# DATABASE_URL=postgresql://user:password@host.neon.tech/neondb?sslmode=require
