PORT=5000
# Keep existing Neon configuration for shared reports.
DATABASE_URL=
ML_SERVICE_URL=http://127.0.0.1:8000
ML_TIMEOUT_MS=8000
# Use the same private key on Node and FastAPI when hosted. Never use VITE_ here.
ML_SERVICE_KEY=
# Optional reviewed geographic zones. Empty means no population assumption.
ML_CONTEXT_ZONES=[]
